// This page is not needed anymore as navigation is handled by middleware.
// The root page is now at /src/app/[lang]/page.tsx.
// We keep this file to avoid a 404 on the root path, middleware will redirect.

export default function Home() {
  return null;
}
